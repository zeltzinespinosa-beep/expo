npm install --global eas-cli && \
npx create-expo-app botonemergenciafmvz && \
cd botonemergenciafmvz && \.
eas init --id f9cf570f-e699-4404-a105-9df30621833d && \
npm install open && \
echo "const { exec } = require('child_process');
const open = require('open');
console.log('🔹 Iniciando build de APK para Android con EAS...');
const buildProcess = exec('eas build --platform android --profile preview');
buildProcess.stdout.on('data', (data) => {
  process.stdout.write(data);
  const urlMatch = data.match(/https:\/\/expo\\.dev\\/accounts\\/[^\\s]+/);
  if (urlMatch) {
    const apkUrl = urlMatch[0];
    console.log('\\n✅ APK listo: ' + apkUrl);
    console.log('🌐 Abriendo enlace en navegador...');
    open(apkUrl);
  }
});
buildProcess.stderr.on('data', (data) => { process.stderr.write(data); });
buildProcess.on('close', (code) => { console.log('\\nBuild finalizado con código ' + code); });
" > build-apk.js && \
node build-apk.js
